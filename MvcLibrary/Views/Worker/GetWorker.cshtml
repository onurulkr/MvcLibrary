@model MvcLibrary.Models.Entity.tblWorkers
@{
    ViewBag.Title = "GetWorker";
    Layout = "~/Views/Shared/Layout.cshtml";
}

@using (Html.BeginForm("UpdateWorker", "Worker", FormMethod.Post))
{
    <div style="margin-bottom:15px"></div>
    <div class="form-group">
        @*@Html.LabelFor(x => x.WorkerId)*@
        <label>Personel Id</label>
        @Html.TextBoxFor(x => x.WorkerId, new { @class = "form-control" })
        <br />
        @*@Html.LabelFor(x => x.WorkerName)*@
        <label>Personel Adı</label>
        @Html.TextBoxFor(x => x.WorkerName, new { @class = "form-control" })
    </div>
    <div>
        <button class="btn btn-warning">Güncelle</button>
    </div>
}

